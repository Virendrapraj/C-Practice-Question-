//Write a C program to read and write data to a file.
#include <stdio.h>

int main() {
    int n;

    printf("Enter a number: ");
    scanf("%d", &n);

    FILE *file;
    fopen(file , "W");

    return 0;
}

